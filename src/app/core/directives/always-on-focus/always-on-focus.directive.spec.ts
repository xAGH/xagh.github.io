import { AlwaysOnFocusDirective } from './always-on-focus.directive';

describe('AlwaysOnFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new AlwaysOnFocusDirective();
    expect(directive).toBeTruthy();
  });
});
